# Dependencies
node_modules/

# Build output
dist/
build/
.vite/

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/

# Blender working files — source .blend files ARE committed,
# these are autosave/backup artifacts only
*.blend1
*.blend2

# Installed tooling under .claude/ — not project source.
# `.claude/launch.json` IS committed (the dev-server config); these are a local
# install of the `impeccable` design skill and are 3.4MB of vendored files.
.claude/skills/
.claude/agents/
.claude/settings.local.json
